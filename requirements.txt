# memcond adds no dependencies of its own. These four are what memllm's cost
# ledger, grader, statistics and BM25 retriever import, and they are enough to
# run the whole test suite and to reproduce every table in the README from the
# stored arms in results/.
pytest
rank_bm25
tiktoken
numpy

# Not required above, and deliberately so:
#
#   huggingface_hub          only to download LongMemEval, which is needed by
#                            scripts/run_mechanical_gate.py and the GPU arms.
#                            tests/ and the offline analysis scripts do not
#                            touch the benchmark.
#
#   torch, sentence-transformers
#                            only for hybrid (dense) retrieval, which nothing
#                            in this repo uses -- every arm here is BM25. See
#                            ../memllm/requirements.txt if you want them.
